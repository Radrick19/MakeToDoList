<UserControl x:Class="ToDoList.View.Components.TaskItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ToDoList.View.Components"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="600">
    <Border BorderThickness="2" BorderBrush="Gray" MaxHeight="50" MinHeight="50">
        <Grid Background="white">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Width="35" Height="35" Margin="10,0"/>
                <Button Width="35" Height="35"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
